#include<iostream>
#niclude<deque>
#include<ctype.h>
#include<string>
using namespace std;
program sample_file;
var
	a,b,ab:string;//string variables
	c,d,cd:int; //integer variables
	e,gee:double; //float type
begin // start of the main code
	a:='hi';
	b:=' there';
	ab:=a+b; /*contact strings*/
	c:=345;
	d:=33333;
	cd:=c+d; /*add integers*/
	e:=3.14;
	gee=c+e; /*add integers*/
	readfromfile[A:textfile.txt];
	for (i=0; i<10;i++)
	begin
		read(string);
		writetofile[A:test, i];
	end;
end. //done